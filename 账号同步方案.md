# V1 学习进度记录 · 账号同步方案设计

> 已实现：**本地进度记录（无需账号）** —— 每次进入应用自动续播上次位置。
> 本文档说明"账号同步"的三种实现方案，供后续决策。

---

## 一、现状（已上线）

### 本地进度记录（无账号版）
- 每 5 秒自动保存 + 暂停/切换/关闭页面时立即保存
- 记录内容：每个视频的**播放秒数、三遍听力的遍数、是否学完（✅ 标记）**
- 存储位置：浏览器 localStorage（key: `leo_v1_progress`）
- 每次进入应用：自动定位到上次视频、恢复到上次秒数与遍数继续播（toast「📖 已续播上次进度」）
- 局限：**只在本设备有效**；清浏览器缓存会丢失；换手机无法同步

---

## 二、为什么"需要账号"？（需求拆解）

| 用户目标 | 本地版是否满足 |
|---|---|
| 每次点进去接着上一次 | ✅ 满足（本设备） |
| 换设备/换手机也能接着学 | ❌ 需要账号同步 |
| 多设备同时登录、进度互通 | ❌ 需要账号同步 |
| 比赛演示时展示"账号体系" | ❌ 需要账号（加分项） |

---

## 三、账号方案对比

### 方案 A：服务器轻后端（推荐）⭐
- **架构**：现有腾讯云服务器上跑一个 Python Flask 小服务 + SQLite 数据库
- **接口**（nginx 反代 `/api/` 到本地端口）：
  - `POST /api/register` — 注册（用户名+密码）
  - `POST /api/login` — 登录，返回 token
  - `POST /api/progress` — 保存进度（带 token）
  - `GET /api/progress` — 拉取进度（带 token）
- **数据表**：
  - `users(id, username, password_hash, salt, created_at)`
  - `progress(user_id, clip_id, time, phase, done, updated_at)`
- **安全**：密码用 `pbkdf2_hmac` 加盐哈希（绝不存明文）；token 用 `secrets.token_hex`；建议后续上 HTTPS
- **前端集成**：首页加"登录/注册"入口 → 登录后进度同时写本地+服务器；启动时若已登录则拉取服务器进度合并（以较新者为准）
- **部署**：systemd 守护 Flask 进程 + nginx 反代
- **优点**：数据在自己手里、无第三方依赖、比赛可现场演示注册/登录/换设备同步
- **成本**：进度数据极小（JSON 几 KB），3M 带宽完全够用

### 方案 B：第三方云存储（LeanCloud / Bmob）
- 用国内 BaaS 的免费额度，前端 SDK 直连，**无需自己写后端**
- 优点：免运维、接入快
- 缺点：依赖第三方服务稳定性；比赛现场万一服务波动影响演示

### 方案 C：Firebase / Supabase
- 国外服务，国内访问不稳定，**不推荐**（用户涉及泰国/国内双环境）

---

## 四、推荐路线

1. **现在**：用本地版（已上线，同一设备体验完整）
2. **比赛前**：如需跨设备同步/账号演示 → 上**方案 A**（约 1-2 天工作量）
   - 后端：Flask + SQLite（一个文件 + 5 个接口）
   - 前端：登录弹窗 + 进度上传/拉取/合并
   - 部署：systemd + nginx 反代，已在服务器上有现成 nginx 配置可扩展

---

## 五、进度合并策略（方案 A 关键细节）

```text
启动时：
  本地进度 L + 服务器进度 S
  → 对每个视频取 updated_at 较新的记录合并
  → last（上次位置）取较新者
保存时：
  本地立即写（离线可用）
  服务器异步上传（登录状态下，防抖 5 秒）
```
这样"断网也能记，联网自动同步"。
